apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kustd.fullname" . }}-test-connection"
  labels:
    {{- include "kustd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kustd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
